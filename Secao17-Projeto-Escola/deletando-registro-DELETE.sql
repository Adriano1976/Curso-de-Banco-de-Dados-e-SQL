-- Se desejarmos remover uma informação de uma determinada linha, usaremos a sintaxe:

DELETE FROM SOBRENOME
WHERE IDALUNO = 1;

-- Se desejarmos remover as informações de uma determinada coluna, usaremos a sintaxe:

DELETE FROM SOBRENOME;